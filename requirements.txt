torch>=2.2
torchvision>=0.17
numpy
scipy
pandas
pillow
scikit-learn
matplotlib
cartopy
xarray
netCDF4
# 日本語を含むパスでもnetCDFを開けるようにするための代替バックエンド(src/era5.py)
h5netcdf
h5py
cdsapi
requests
beautifulsoup4
pdf2image
# 天気図の色マスクと記号のテンプレートマッチング(src/chartsymbols.py)。
# 画面表示は使わないので headless 版でよい。GUI版は libGL を要求するため、
# 画面の無い環境(CI・コンテナ)では import の時点で落ちる。
opencv-python-headless
fastapi
uvicorn[standard]
python-multipart
tqdm
ipywidgets
# テスト(tests/)。python -m pytest で実行する
pytest

# ラベリングはノートブック上で動く（notebooks/labeling.ipynb）
jupyter
ipykernel
