# 天気図画像とERA5格子の比較 — 到達点と、未解決のこと

2026-08-21。ブランチ `claude/val-split-and-calibration`。

このファイルは、後から読む人(数か月後の自分を含む)が、会話のログを持たない状態で
同じ地点から再開できるように書いている。数字は再現に必要な条件つきで載せる。

> **数字は `data/labels_v2.csv` で測ったもの**(runs/v2_chart・runs/v2_grid)。
> `labels.csv` で測った旧版との違いは「ラベルを直して何が変わったか」に書いた。

## 次にやること

`--val-mode spread` での確認は完了した(下記)。残っているのは:

1. **残り8ラベルの再現性を測る**。okhotsk_high は人間でもF1 0.50、
   stationary_front は0.98(下記)。**同じ0.4でも意味がまったく違う。**
   報告する数値には、そのラベルの上限を併記したい。1ラベルにつき1か月分
   (60枚・10分程度)を `scripts/review_cli.py` で判定し、`scripts/compare_review.py`
   で突き合わせる。
2. **ERA5のチャンネル追加**(いまは海面更正気圧と850hPa気温だけ)。前線が格子で
   弱い(0.442対0.656)のは「記号だから無い」のか「気圧と気温だけでは判断できない」
   のか、まだ切り分けていない。500hPa高度・風・相当温位を足せば分かる。
   **現在の結論の解釈に直接関わる。**
3. **futatsudama_low の判定基準を `src/labels.py` に書く**(下記)。
4. **2000〜2022年のラベリング**。天気図15,522枚は取得済み、ERA5も取得可能。
   希少ラベル(futatsudama_low 112件、okhotsk_high 199件)にはこれ以外の道がない。

### このページの数字を測ったあとで、ラベルを26件直した

- `apply_label_rule` で9件(futatsudama_low に個別の低気圧ラベルが併記されていた分)
- `apply_review` で6件(futatsudama_low の盲検レビューで yes とした分。13件中6件)
- `apply_review` で11件(stationary_front。2025年4月10件・7月1件の付け忘れ)

2401件中26件(1.1%)。`runs/v2_chart_spread` / `runs/v2_grid_spread` は前半15件の
修正までを含み、stationary_front の11件は含んでいない。

### futatsudama_low の判定基準は、まだ言語化されていない

13件の盲検レビューは 6 yes / 7 no / **0 unsure** に割れた。迷いなく割れたということは、
「日本海と南岸に低気圧がある」だけでは決まらず、勢力の均衡や同時性といった条件で
判断しているということ。**その条件がどこにも書かれていない。** 判定した本人が
覚えているうちに `src/labels.py` に書き足すこと。

## ラベルの信頼性は、ラベルによって大きく違う

同じ人が答えを伏せてもう一度判定したときの一致度(`scripts/compare_review.py`)。

| ラベル | 対象 | Cohenのκ | 人間どうしのF1 |
|---|---|---|---|
| stationary_front | 2024年7月(62枚) | **0.912** | **0.979** |
| stationary_front | 2025年4・7月(122枚) | 0.714 | 0.766 |
| okhotsk_high | 抜き取り200枚 | 0.464 | 0.500 |

**okhotsk_high は人間でもF1 0.50しか一致しない。** 格子モデルの0.396(spread)は、
その上限に対してかなりの位置にある。「モデルの性能が足りない」ではなく
「ラベルの再現性が天井を作っている」と読むべきラベル。

**stationary_front は逆に、1年以上あけても0.979で一致する。** モデルの0.803には
まだ伸びしろがある。2025年の値(0.766)が低いのは、4月のラベル付けだけが雑だった
ため(下記)。

**報告するときは、この上限と並べること。** 同じ0.4でも、天井が0.5のラベルと
0.98のラベルでは意味がまったく違う。まだ測っていないラベルが8つある。

### 2025年4月の停滞前線だけが、付け忘れていた

年ごとの出現率(33.6% / 37.1% / 19.8%)を見て「2025年のラベルが信用できない」と
考えたが、**それは誤りだった**。

月ごとに見直した結果:

| | 既存ラベル | 見直し後 |
|---|---|---|
| 2024年7月 | 76%(47件) | **76%(47件)** ← 完全一致 |
| 2025年7月 | 27%(17件) | 29%(18件) |
| 2025年4月 | 2%(1件) | **18%(11件)** ← 10件不足 |

**2024年の76%も2025年の27%も、どちらも正しい。** 2024年は梅雨が長く、2025年は
7月20日頃に明けた。年ごとの差は気候の変動であって、基準のずれではない。

直したのは2025年4月の10枚と7月の1枚、合計11枚だけ(`scripts/apply_review.py`)。
当初見積もった「2023・2024年を含む約500件の付け直し」は不要だった。

**教訓**: 年単位の集計で異常に見えても、月単位まで下りて、かつ他の年を同じ基準で
測り直さないと、ラベルの問題か気象かは決まらない。`scripts/label_stats.py --by-month`
と、他の年の同じ月を`scripts/review_cli.py`で測り直すのが、その手順。

## 問い

天気図の画像と、その元になっているERA5の気圧場。気圧配置の分類にはどちらが有効か。
出発点の仮説は「格子は天気図の元データなのだから上位互換のはず」だった。

**この仮説は否定された。** ただしラベルによっては逆になる。

## 結論

leave-one-year-out 交差検証(2023/2024/2025)、macro F1。

`--val-mode spread`(検証もテストも通年)、`data/labels_v2.csv`。

| 入力 | macro F1 | 自明な予測との差 | fold間の標準偏差 |
|---|---|---|---|
| ERA5格子 | 0.503 | +0.236 | 0.011 |
| 天気図画像 | 0.636 | +0.370 | 0.022 |
| 両方(確率を混ぜる) | **0.646** | **+0.380** | 0.030 |

**単独では天気図が勝つが、両方使うのが最良**(「組み合わせ」を参照)。
`--val-mode tail` でも同じ順序になる(0.512 / 0.626 / 0.640)。

「自明な予測」は、全部を陽性と答えるだけで得られる macro F1 (この10ラベルでは0.258)。
**macro F1 の絶対値だけを見てはいけない。** 出現率pのラベルは何もしなくても 2p/(1+p) を取る。

前線2ラベル(front_passage / stationary_front)は格子に含まれない情報なので、除いても比べた。

| 入力 | 全ラベル | 前線2つを除外 |
|---|---|---|
| 天気図 | +0.359 | +0.357 |
| ERA5格子 | +0.245 | +0.268 |
| 差 | −0.114 | **−0.089**(全foldで同符号) |

前線で説明できるのは0.025、全体の2割ほど。**天気図の優位の大半は前線記号ではない。**
天気図は前線を除くと0.626→0.599と落ちるが、格子はほぼ動かない(0.512→0.510)。

除外後の基準も同じ8ラベルで取り直している(前線は出現率が高く、停滞前線は3割あるので、
片側だけ10ラベルの基準と比べると上積みを過大評価する)。

### ラベルごとには、得意分野が分かれる

`--val-mode spread`、`labels_v2.csv`。

| ラベル | 天気図 | ERA5格子 |
|---|---|---|
| 停滞前線 | 0.803 | 0.596 |
| 西高東低 | 0.797 | 0.711 |
| 移動性高気圧 | 0.726 | 0.709 |
| 台風 | 0.707 | 0.558 |
| 日本海低気圧 | 0.668 | 0.402 |
| 前線通過 | 0.656 | 0.442 |
| 南岸低気圧 | 0.605 | 0.425 |
| 太平洋高気圧型 | 0.560 | 0.502 |
| 二つ玉低気圧 | 0.518 | 0.286 |
| **オホーツク海高気圧** | **0.318** | **0.396** |

低気圧・台風・前線は天気図の圧勝、大規模な高気圧と冬型は互角、オホーツク海高気圧
だけ格子が優る。**この逆転は3つのfoldすべてで起きる**(格子 0.52/0.24/0.42 に対し
天気図 0.39/0.19/0.38)ので、ばらつきの範囲ではない。`tail` でも同じ
(格子 0.50/0.43/0.48 対 天気図 0.30/0.33/0.33)。

オホーツク海高気圧は「オホーツク海にある」という位置が定義そのもので、等圧線の形に
特徴が乏しい。Grad-CAMでも、天気図モデルは高気圧本体ではなく下流の等圧線を見ていた。
格子側は座標チャンネル(`--coordconv`)で位置を直接参照できる。

## 選択の偏りを、手つかずの年で確かめた

構造・幅・CoordConv・選択指標・閾値・混合の重み、そしてラベルの改訂まで、すべて
2023〜25年の交差検証を見ながら決めている。その数字には楽観方向の偏りが入りうる。

`labels_v2.csv` には2026年が234件あり、今日の選択には一度も使っていない。
2023〜25年で学習して2026年で評価した(`runs/holdout2026`)。

| | macro F1 | 自明な予測 | 上積み |
|---|---|---|---|
| 交差検証 2023〜25年 | 0.626 | 0.267 | **+0.359** |
| 2026年(手つかず) | 0.593 | 0.235 | **+0.358** |

絶対値の差はラベル分布の違いによるもので、**上積みはほぼ一致する。選択の偏りは小さい。**

ただし2026年は年の途中まで(冬〜春)しかない。

- typhoon は0件で評価対象外。pacific_high も2件しかなく、そのF1(0.50)は平均に
  入れる意味がない。**この2つを除いた8ラベルで +0.341** と読むのが誠実。
- **前線2つが大きく落ちる**(front_passage 0.718→0.41、stationary_front 0.749→0.52)。
  一方で冬型や日本海低気圧はむしろ良い(0.754→0.79、0.645→0.83)。閾値を5〜12月の
  検証データで決めて1〜4月のテストに当てているためと考えられる。**検証の季節偏り
  (下記の欠陥3)が、また別の形で出ている。**

## 測り方の欠陥を3つ直した。直す前の数字は使えない

**この3つを直す前に出た数値は、すべて信用できない。** 特にERA5格子側は数字が
0.473 → 0.234 と激変した(天気図側は 0.613 → 0.619 でほぼ変わらなかった)。

### 1. モデル選択の指標が、報告する指標と別物だった

ベストエポックの判定に閾値0.5固定の macro F1 を使い、最終評価では閾値を最適化していた。
pos_weight で陽性寄りに学習させる以上、初期エポックは何でも0.5を超えて高いF1が出て、
学習が進んで予測が鋭くなるほど0.5超えが減る。**エポック1が最高値を取り、patienceで
エポック9に打ち切られ、ほぼ未学習の重みが保存されていた**(3つのfoldすべてで発生)。

閾値に依存しない macro AP で選ぶように変更(`--select-metric macro_ap`、既定)。

### 2. 自明な予測との比較をしていなかった

macro F1 0.250 を「低いが学習はできている」と読んでいたが、自明な予測は0.258。
**下回っていた。** `src/evaluate.py` の `trivial_macro_f1` が毎回出力するようにした。

### 3. 検証データが8〜12月に偏っている(影響は残るが、当初より小さい)

`--val-mode tail` は学習期間の末尾を検証に回すため、検証が5か月・テストが12か月になる。
この偏りは今日3回、別々の形で問題を起こした。

- 224格子でval_lossが2.9〜121の間を暴れた
- val_apがエポック24を最良としたが、テストではエポック1の重みのほうが良かった
- 天気図と格子を混ぜる重みの選択が破綻した(下記)

`--val-mode spread` が用意されているが、まだこれで取り直していない。

ただし okhotsk_high については、ラベルを直して陽性が188件(8.7%)になったことで
検証データにも現れるようになった(`scripts/preflight` が警告を出さない)。
上の3件はいずれも陽性85件の頃の観測なので、いまどれだけ残っているかは未確認。

## ERA5格子側は、モデルを作り直した

EfficientNet-B0 をそのまま流用すると、正しく測れば自明な予測を下回る(0.234)。
`--arch small_cnn`(`src/model.py` の `SmallCNN`)に替え、`--coordconv` を足して 0.497。

**当初これを容量の問題と診断したが、誤りだった。** 幅を振ると性能は6万から385万
パラメータまで単調に上がる。EfficientNet-B0 とほぼ同じ385万パラメータの浅いネットが
0.497、EfficientNetが0.234。**容量を揃えても結果が正反対なので、効いているのは構造。**

| 幅 | パラメータ | macro F1 |
|---|---|---|
| 16 32 64 64 | 61k | 0.386 |
| 32 64 128 128 | 242k | 0.424 |
| 48 96 192 192 | 543k | 0.441 |
| 64 128 256 256 | 964k | 0.452 |
| 96 192 384 384 | 2.17M | 0.481 |
| **128 256 512 512** (既定) | **3.85M** | **0.497** |
| 160 320 640 640 | 6.01M | 0.502 (ばらつきが増える) |
| EfficientNet-B0 | 4.02M | 0.234 |

EfficientNet側の何が悪いのかは**切り分けていない**。候補は、深さ(16ブロック対4段)、
気圧場に転移しないImageNet重みからの出発、2チャンネル入力では意味をなさないSEブロック。

なお格子の解像度は、既定の128がERA5の元データ(181×221点)より粗い。224に上げると
むしろ悪化したが、それは当時EfficientNetを使っていたためで、SmallCNNでは試していない。

## 組み合わせ

### 両モデルの確率を混ぜる(`scripts/ensemble_chart_grid.py`)

`--val-mode spread`、`labels_v2.csv`。

| | macro F1 | 自明な予測比 | fold間の標準偏差 |
|---|---|---|---|
| 天気図のみ | 0.636 | +0.370 | 0.022 |
| ERA5格子のみ | 0.503 | +0.236 | 0.011 |
| 混合(一律の重み) | 0.642 | +0.376 | 0.027 |
| **混合(ラベルごとの重み)** | **0.646** | **+0.380** | 0.030 |

天気図単独を+0.010上回る(fold別 +0.021 / +0.008 / +0.001、3foldとも正)。

**効いている理由は、当初の説明とは違った。** 「得意分野が逆のラベルを互いに補う」
つもりだったが、伸びているのは両者とも中程度で違う誤りをしているラベルのほう。

| ラベル | 天気図 | 格子 | 混合 | 上積み | 重み |
|---|---|---|---|---|---|
| migratory_high | 0.726 | 0.709 | 0.748 | +0.023 | 0.68 |
| nankigan_low | 0.605 | 0.425 | 0.626 | +0.021 | 0.25 |
| front_passage | 0.656 | 0.442 | 0.673 | +0.017 | 0.17 |
| **okhotsk_high** | 0.318 | **0.396** | 0.378 | **−0.019** | 0.68 |

**okhotsk_high は混ぜると格子単独より下がる。** `tail` では重み0.93(ほぼ格子のみ)が
選ばれて格子の性能を保っていたが、`spread` では0.68に留まり天気図が足を引っ張る。
**このラベルは混ぜずに格子だけを使うのが最良。**

学習済みの重みを使うので再学習は不要。重みと閾値はどちらも検証データで決め、
テストには一度も触れずに適用する。**重みはラベルごとに決めること** -- 一律だと
得意分野が逆のラベルが妥協させられる(下記)。

#### 旧ラベル(labels.csv)では効かなかった

同じ手順を旧ラベルで走らせたときは、混合(ラベルごと)が0.617で天気図単独の0.619に
届かなかった。okhotsk_highの重みがfoldごとに0.20/0.75/0.20とばらついたのが原因で、
陽性85件では8〜12月の検証データにこの型がほとんど現れず、**重みを選ぶ材料が無かった**。

陽性が199件になって検証にも現れるようになると、`tail` では重みが0.85/0.95/1.00で
安定した。`scripts/preflight` が「検証データに1件も現れないラベル」を警告するのは、
この失敗が理由。

**特徴ベクトルの連結は採らない。** `scripts/ensemble_era5.py` に記録があるとおり、
ERA5の数値をCNNの特徴に連結したときは次元比(1280対17)のせいで信号が埋もれた。

## うまくいかなかったこと

### ImageNetの事前学習を格子に転用する

最初の畳み込みだけ作り直して残りを引き継ぐ実装は入れた(`_adapt_first_conv`)。
EfficientNetでは効果がなかった(0.473→0.473)。滑らかな気圧場には自然画像の
エッジ・質感が転移しないという解釈。

## 未解決のこと

### (解決済み) ラベルの版

当初の実験は `data/labels.csv` で回していた。その翌日の版が手元にあったが、
gitに入っておらず(追跡されていたのは `labels.csv` だけ)、READMEにも記載がなく、
結果JSONにもどのラベルで測ったかが残っていなかったため、丸一日気づかなかった。

```
labels.csv                2026/08/19 14:17   ← 当初使っていたのはこちら
review_okhotsk_full.csv   2026/08/20 15:55
review_okhotsk_final.csv  2026/08/20 16:34
labels_v2.csv             2026/08/20 16:52
```

オホーツク海高気圧の陽性数が違う。

| | okhotsk_high 陽性 | 全行 |
|---|---|---|
| labels.csv | 85 | 2432 |
| labels_v2.csv | **199** | 2432 |

差は2.3倍。ラベルが変わった行は128件で、そのほとんどがこのラベル。
**つまりこのページの数字は、真の陽性の6割近くを見落としたラベルで測られている。**

影響が最も大きいのは当然オホーツク海高気圧(天気図0.131 / 格子0.345)だが、
macro F1 は全ラベルの平均なので、総合値 0.619 / 0.497 も動く。

**取り直した。このページの数字は `labels_v2.csv` のもの。** `labels_v2.csv` と
見直しの記録(`review_okhotsk*.csv`)はgitに入れた。

学習の前に `python -m scripts.preflight --labels <ファイル> --years ...` を流せば、
どのラベルファイルに何件入っているかが数秒で分かる。今回はこれが無かったために
丸一日ぶんの実行を古いラベルでやり直すことになった。

#### ラベルを直して何が変わったか

okhotsk_high の陽性が 85 → 199(同じ2432行)。

| | labels.csv | labels_v2.csv |
|---|---|---|
| 天気図 全体 | 0.619 | 0.626 |
| ERA5格子 全体 | 0.497 | 0.512 |
| 天気図 okhotsk_high | 0.131 ± 0.081 | **0.319 ± 0.019** |
| ERA5格子 okhotsk_high | 0.345 ± 0.150 | **0.470 ± 0.033** |

天気図のokhotsk_highは2.4倍になった。**0.131という値はモデルの能力ではなく、
ラベルの欠落を測っていた。** fold間の標準偏差も両方とも4分の1以下になり、
初めて報告できる数字になった。

一方で全体の差(0.122 → 0.114)と、okhotsk_highで格子が上回るという関係は変わらない。
**ラベルの不備で説明できたのは差の一部だけで、残りは実際の性能差。**

### 2. オホーツク海高気圧のラベルには、それでも見落としが残りうる

`scripts/label_tool.py` の `positives_union` は「一度でも陽性とした」天気図だけを
見直しの対象にする。**一貫して見落とされてきた事例は永久に陰性のまま。**
陽性23〜36件という数は、実際の出現頻度より少ない可能性がある。

### 3. 希少ラベルは、平均を報告できる状態にない

出現数23〜36件のラベルは、最良の構成でもfold間で倍以上ばらつく。

ラベルを直したことで okhotsk_high は安定した(格子 0.50/0.43/0.48)。残るのは
futatsudama_low で、天気図 0.433 ± 0.135(0.28 / 0.49 / 0.53)と10ラベル中もっとも
ばらつく。陽性92件。

**このラベルは見直しを受けていない。** okhotsk_high が85→199で挙動が一変した前例が
あるので、まずラベルの品質を疑うこと(`scripts/label_tool.py` の盲検レビューが使える)。
2つの低気圧の位置関係で決まる型なので、判定基準は揺れやすいはず。

ラベルが妥当だと確認できてなお不安定なら、そこから先はデータ量の問題。

`awg-yk/weather-pattern-classification-data` に**2000-01-01〜2022-09-30の天気図が
既にある**(国会図書館版)。ERA5も同期間ダウンロード可能。**必要なのはラベルだけ。**
23年分あれば希少ラベルも10倍前後になる。

## 再現に使うコマンド

```bash
# 学習の前に。設定の不備を数秒で洗い出す
python -m scripts.preflight --data-dir <画像> --labels data/labels_v2.csv \
    --years 2023 2024 2025

# 天気図
python -m scripts.cross_validate --data-dir <画像> --labels data/labels_v2.csv \
    --years 2023 2024 2025 --out-dir runs/v2_chart

# ERA5格子
python -m scripts.cross_validate --input-mode era5-grid --arch small_cnn --coordconv \
    --labels data/labels_v2.csv --years 2023 2024 2025 --out-dir runs/v2_grid

# 比較(ラベルを絞って平均を取り直せる。基準も同じラベルで取り直される)
python -m scripts.compare_runs runs/v2_chart runs/v2_grid \
    --exclude front_passage stationary_front

# 混合(学習済みの重みを使う。再学習は不要)
python -m scripts.ensemble_chart_grid --data-dir <画像> --labels data/labels_v2.csv \
    --chart-weights runs/v2_chart --grid-weights runs/v2_grid \
    --years 2023 2024 2025 --out runs/v2_ensemble.json
```

`summary.json` には実行時の設定が記録される(それ以前の結果には無いので、
`compare_runs` は「設定の記録なし」と表示する)。各foldの学習経過は
`<重み>.history.json` に残る。
