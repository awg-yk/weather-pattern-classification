# 保存済みラベルは、妥当な答えを取りこぼしている(2026-08-25)

2023年のテストセット60枚について、モデルの1位判定に人が○×を付け、
`data/labels_v2.csv` の判定と突き合わせた。

## 測ったこと

|  | ラベル上も正解 | ラベル上は不正解 |
|---|---|---|
| **人が ○** | 52 | **5** |
| **人が ×** | **0** | 3 |

- ラベルで測った正解率: **86.7%**
- 人の目での正解率: **95.0%**
- 取りこぼし: **5/60 = 8.3%**(95%区間 3.6%〜18.1%)

**逆向き(ラベル上は正解だが人は×)が0件。** 一方向であることが重要で、
これは「基準が違う」でも「人が甘い」でもなく、ラベルが妥当な答えを
落としている、という仮説だけが説明できる形になっている。

判定は「日本全域の気圧配置として妥当か」で行った。風替わりの日の選定に使った
「小松周辺の風替わりに効くか」という基準ではない。ラベル付けと同じ問いである。

## 原因の一つは、二つ玉低気圧の付け方

`data/labels_v2.csv` 全体を数えると:

```
二つ玉あり                        : 112件
  うち 日本海低気圧を併記          :   0件
  うち 南岸低気圧を併記            :   0件
```

**112件すべてで構成要素が併記されていない。例外はない。**
`scripts/label_stats.py` が記録しているとおり、これは意図的な運用
(個別の2ラベルを足すのではなく、置き換える)である。

しかし二つ玉低気圧は「日本海低気圧と南岸低気圧が同時にある」状態を指すので、
**天気図にはその両方が実際に写っている**。モデルが片方を答えると、正しく
読み取っているのに不正解と数えられる。取りこぼし5件のうち2件がこれだった。

```
2023-01-20 12Z  モデル=二つ玉低気圧(61%)  / 記録=前線通過|日本海低気圧
2023-06-22 00Z  モデル=南岸低気圧(100%)   / 記録=二つ玉低気圧|停滞前線
```

残る3件は、この規約とは無関係な単純な付け忘れだった。

```
2023-08-23 00Z  モデル=移動性高気圧(73%)  / 記録=太平洋高気圧型
2023-05-18 12Z  モデル=南岸低気圧(99%)    / 記録=移動性高気圧|停滞前線
2023-05-14 00Z  モデル=移動性高気圧(57%)  / 記録=二つ玉低気圧
```

## 影響

**報告している macro F1 は下限である。** どれだけ下振れしているかは、
top-1正解率での8.3%からそのまま換算できない(F1はラベルごとの適合率・再現率で
決まり、取りこぼしは主に適合率を押し下げる)。ただし向きは確実に一方向で、
実力を過小評価する側に働いている。

**確信度の校正も、この偏りを引き継いでいる。** 校正は labels_v2.csv を
正解として当てはめたので、「妥当なのに不正解と数えられた分」だけ確信度を
下げる方向に学習した。実際、2023年60枚では校正後の確信度が人の目に対して
低すぎる(50〜70%と表示した7件が実際には100%正しい)。

出発点の「明らかに違うのに確信度が高い」は、少なくとも日本全域の気圧配置と
いう基準では、この評価では起きていない。

## 規約を変えた場合の影響を測った

`scripts/apply_label_rule.py --add` で「二つ玉なら構成要素も併記する」版のラベルを
別ファイルに作り、同じ重み(`runs/v2_chart_spread/model_test2023.pt`、2023年をテスト)で
評価し直した。

| | 現行(排他) | 合成とみなす | 差 |
|---|---|---|---|
| macro F1 | 0.665 | 0.671 | +0.006 |
| 南岸低気圧 F1 | 0.61 | 0.65 | +0.04 |
| 日本海低気圧 F1 | 0.63 | 0.66 | +0.03 |
| 1位正解率 | 82.4% | 84.0% | +1.6pt |
| ECE(校正後) | 0.036 | 0.027 | -0.009 |

他の8ラベルは一切動かない。**規約の影響は実在するが小さい。**
macroは10ラベルの平均なので、2ラベルが0.03〜0.04動いても全体では+0.006にとどまる。

そして重要なのは、**これが取りこぼしの一部しか説明しないこと**である。
1位正解率のズレ8.3ptのうち、規約変更で回復したのは1.6pt —— **約5分の1**。
残る6.7ptは二つ玉とは無関係な単純な付け忘れである。

## 次に狙うべきは、適合率の低いラベル

| ラベル | 適合率 | 再現率 | F1 |
|---|---|---|---|
| オホーツク海高気圧 | 0.29 | 0.59 | 0.39 |
| 二つ玉低気圧 | 0.39 | 0.53 | 0.45 |
| 太平洋高気圧型 | 0.47 | 0.72 | 0.57 |

低い適合率は、ラベルの取りこぼしがまさに現れる形である。モデルが「オホーツク海
高気圧」と答え、記録に無いから誤検出と数えられる —— それが71%を占める。
その多くが実際には妥当なら、このラベルの実力は0.39よりずっと高い。

無作為に60枚見ると当たりは5枚だったが、**誤検出だけを狙えば全部が候補になる**。
同じ労力で桁違いに情報が得られる。

## オホーツク海高気圧では、取りこぼしは起きていなかった

適合率が最も低いオホーツク海高気圧(0.29)について、誤検出だけを採点した。
2023年テスト・校正済み確率・しきい値0.35で、モデルが主張した21枚のうち
記録に無い9枚を全部見た。

**9枚すべてが本当に誤りだった(妥当0枚)。** 適合率は組み直しても 0.571 のまま動かない。

このラベルについては、3つの仮説がすべて否定された:

- **動作点の選び方** —— `--calibrated` を付けて評価し直しても macro F1 は
  0.665 → 0.663 でほぼ不変、okhotsk_high も 0.29/0.59/0.39 → 0.29/0.65/0.40 で変わらない
- **ラベルの取りこぼし** —— 誤検出9枚すべてが本物の誤り
- **隠れた実力** —— しきい値0.35なら適合率0.571まで上がるが、再現率は12/68=0.18に落ちる。
  F1にすると0.27で、報告値の0.40より悪い

**報告値 0.29 は正しい。** モデルが本当に弱いラベルであり、
`docs/2026-08-22-next-chart-only.md` の「オホーツク海高気圧は天井に近い」という
判断が独立に裏付けられた。

なお、この検証の副産物として **校正がF1を変えないことが実データで確認できた**
(macro F1 0.665 → 0.663、差はしきい値探索が0.05刻みであることによる)。
`tests/test_calibration.py::test_optimized_f1_is_unchanged_by_calibration` の
主張が、合成データだけでなく実データでも成り立っている。

## まだ分かっていないこと

取りこぼし8.3ptのうち、二つ玉の規約で説明できるのは1.6pt、オホーツク海高気圧では
0件だった。**残りがどのラベルに散っているかは未特定。**

同じ手順(`--focus-label` + `--only-false-positives`)を、適合率の低い残り2つに
当てることで確かめられる。

| ラベル | 適合率(校正後) | 再現率 | F1 |
|---|---|---|---|
| 二つ玉低気圧 | 0.40 | 0.50 | 0.44 |
| 太平洋高気圧型 | 0.53 | 0.67 | 0.59 |

どちらも誤検出が本物なら、取りこぼしは特定のラベルに偏っておらず薄く散っている
ことになり、報告値はおおむね正しいという結論になる。

## 決める必要があること

二つ玉低気圧を**合成ラベル**とみなすのか、**排他的な分類**とみなすのか。

- 合成とみなす → 112件に日本海低気圧・南岸低気圧を併記する。
  日本海低気圧 419→531件、南岸低気圧 255→367件になり、報告値はすべて出し直し
- 排他とみなす → ラベルは変えず、評価側で「二つ玉と構成要素は矛盾しない」と
  みなす。ラベルは動かないが、指標の定義が複雑になる
- 何もしない → 報告値は下限であると明記する

これは気象学的な定義の問題であって、実装で決められるものではない。

## 測り直す手順

```bash
python -m scripts.predict_charts --images-dir <画像> --labels data/labels_v2.csv \
    --weights runs/v2_chart_spread/model_test2023.pt --years 2023 --limit 60 \
    --out data/predictions_2023.csv

python -m scripts.grade_predictions --predictions data/predictions_2023.csv \
    --images-dir <画像> --out data/grade_2023.csv --against-labels data/labels_v2.csv
```

leave-one-year-out の重みでは、`--years` にその重みのテスト年だけを指定すること。

n=60 は取りこぼし率の区間が 3.6%〜18.1% と広い。方向は確定しているが、
大きさを詰めるには枚数がいる。
